import {OPEN_SIDE_NAV} from './actions';

const reducer = (state = [], action) => {
  switch (action.type) {
    case OPEN_SIDE_NAV:
      return state
    default:
      return state
  }
}

export default reducer;
